import { DescriptedCode } from '@smartb/g2-documentation'
import ProjectEndpointDescLeft from './descLeft.md'
import ProjectEndpointDescRight from './descRight.md'

<DescriptedCode
  leftElement={<ProjectEndpointDescLeft />}
  leftContainerProps={
    {
      width: "50%"
    }
  }
  rightElement={<ProjectEndpointDescRight />}
  rightContainerProps={
    {
      width: "50%",
      position: "relative",
      paddingTop: "0"
    }
  }
/>

---
